<% provide(:title, "投稿の編集") %>
<h1 class="mt-4 text-center">投稿の編集</h1>
<div class="container mt-5">
  <div class="row">
    <div class="col-6 mx-auto">
      <%= render 'form', animal: @animal %>
    </div>
  </div>
</div>

